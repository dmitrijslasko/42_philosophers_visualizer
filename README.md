School 42 Philosophers visualizer built in Raylib
Currently runs via a shared array + separate thread for the visuals
Ideally, the visually has to made into a separate binary that communicates with the philosophers project via pipe
